# 

# 1. Advanced decorators
## 1.1 `dec_1.py`
1. we see how to provide `*args` and `**kwargs` to decorator
2. `is_authenticated_decorator` to check if a user is logged in with help of decorators

## 1.2 'dec_2.py'
- Create a `logging_decorator()` which is going to log the name of function, arguments given and returned o/p

# 2. Flask

## 2.1 `hello.py`
- a flask app
- with custom decorators 
    1. `make_bold` **bold** the return statement of a route
    2. `make_emphasis` _italic_ the return statement of a route
    3. `make_underlined` underline the return statement of a route

## 2.2 `server.py`
- a flask app
- Guess a number betn 0 and 9 